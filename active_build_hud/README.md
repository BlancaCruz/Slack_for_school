# Priority Funnel HUD — The Stitch Blueprint

Summary
- Mobile-width HUD mockup focused on a "Priority Funnel" sidebar, sprint chat area, and environment metrics.

Tech
- HTML + Tailwind CSS (CDN)
- Google Fonts (`Inter`), Material Symbols

How to view
- Open [code.html](code.html) in a browser.

Notes
- Uses remote images and icons; designed at ~430px max width for a phone-like preview.
- Screenshot: [screen.png](screen.png)

Quick open (from repo root):

```bash
open active_build_hud/code.html active_build_hud/screen.png
```
